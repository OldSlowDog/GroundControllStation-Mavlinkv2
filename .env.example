# ============================================================
# 地图 API 密钥配置模板
# 使用方法：复制本文件为 .env，并填入你的真实 Key
# .env 已加入 .gitignore，不会被提交到仓库
# 申请地址: https://lbs.qq.com/
# ============================================================

# ── 腾讯地图 JavaScript API GL Key（用于地图渲染） ──
# 申请路径: 控制台 → 应用管理 → 我的应用 → 新建应用 → 添加 Key（类型选 WebService/JS API）
MAP_JS_KEY=

# ── 腾讯地图 WebService API Key（用于 POI 搜索 / 地点检索） ──
# 申请路径: 控制台 → 应用管理 → 我的应用 → WebService Key
MAP_WS_KEY=

# ── 腾讯地图 WebService 签名密钥（SK）（用于请求签名，非常敏感） ──
# 在 "应用管理 → Key 配置" 页面可以看到 SK（启用签名校验后显示）
# ⚠️ SK 属于敏感信息，绝不可提交到公开仓库
MAP_WS_SK=
